.container {
  display: block;
  overflow: scroll;
  height: calc(100vh - 64px - 72px);
  overflow-x: hidden;
}

.post {
  background-color: green;
  margin: 0 20px;
}

.addButton {
  position: absolute;
  right: -96vw;
  bottom: 10px;
}

