.container {
  overflow: scroll;
  height: calc(100vh - 64px - 72px);
  overflow-x: hidden;
}

.post {
  background-color: green;
  margin: 0 20px;
}

.addButton {
  position: absolute;
  bottom: 10px;
  right: 10px;
}

