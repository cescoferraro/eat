export let initialDashboardReducer = {
    tab: 1
};

export const DashboardReducer = (state, action: any = {}) => {
    switch (action.type) {
        default:
            return {...state}
    }
};
